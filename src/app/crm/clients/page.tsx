import { ComingSoon } from "../coming-soon";

export const metadata = { title: "Clients" };

export default function ClientsPage() {
  return (
    <ComingSoon title="Clients" phase="Phase 2 — port the prototype">
      Client records, per-client quote history and the pipeline that the public
      lead form feeds into (Phase 5).
    </ComingSoon>
  );
}
