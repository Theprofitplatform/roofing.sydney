
## Task tracking & multi-agent coordination (Claude Code + Codex)

**Before starting or resuming ANY work item, read `.planning/TRACKER.md`** and follow the lock protocol at the top of that file — claim the work item (and a lane lock for any shared live system) via `.planning/scripts/tracker-lock.sh` before mutating anything, and never proceed when the script refuses. TRACKER.md is the single task list for this repo — do not use task-journal/`tasks.json` or other task stores.
